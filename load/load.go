package load

func ReturnFormattedData(userSelectOne string) (string, error) {
	//this function takes the data the user wants to search on
	//will need to take in more than the selection but ??? how
	//search the data
	//format it
	//return formatted data

	formattedData := "this is where the display comes back"
	return formattedData, nil

}
